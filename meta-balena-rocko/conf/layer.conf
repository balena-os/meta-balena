BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "balena-rocko"
BBFILE_PATTERN_balena-rocko := "^${LAYERDIR}/"
BBFILE_PRIORITY_balena-rocko = "1337"
