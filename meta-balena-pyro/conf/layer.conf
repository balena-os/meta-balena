BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "balena-pyro"
BBFILE_PATTERN_balena-pyro := "^${LAYERDIR}/"
BBFILE_PRIORITY_balena-pyro = "1337"
