BBPATH .= ":${LAYERDIR}"

BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "balena-sumo"
BBFILE_PATTERN_balena-sumo := "^${LAYERDIR}/"
BBFILE_PRIORITY_balena-sumo = "1337"

LAYERSERIES_COMPAT_resin-sumo = "sumo"
